const assert = require('assert');
// const reactComponentGenerate = require('../index.js');

describe('reactComponentGenerate', () => {
  it('has a test', () => {
    assert(true, 'reactComponentGenerate should have a test');
  });
});
