const assert = require('assert');
// const reactComponentBuilder = require('../index.js');

describe('reactComponentBuilder', () => {
  it('has a test', () => {
    assert(true, 'reactComponentBuilder should have a test');
  });
});
